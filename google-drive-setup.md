# Google 雲端硬碟同步 — 設定步驟

要讓白板把資料存進你的 Google 雲端硬碟，需要先跟 Google 申請一組「OAuth 用戶端 ID」。
整個過程大約 10 分鐘，全部在 Google Cloud Console 裡完成，不用付費。

申請完你只會拿到**一組用戶端 ID**，把它貼進白板的 ☁️ 面板就完成了。
用戶端 ID 可以公開，它不是密碼。

---

## 1. 建立專案

1. 打開 https://console.cloud.google.com
2. 用你的 Google 帳號登入
3. 點畫面**最上方左邊**的專案選單（在「Google Cloud」字樣旁邊）
4. 點右上角的 **新增專案**
5. 專案名稱隨便取，例如 `worldbuilder`，按 **建立**
6. 等幾秒鐘建立完成後，**確認上方的專案選單已經切到這個新專案**

---

## 2. 啟用 Google Drive API

1. 左上角漢堡選單 ☰ → **API 和服務** → **程式庫**
2. 搜尋 `Google Drive API`
3. 點進去，按 **啟用**

---

## 3. 設定 OAuth 同意畫面

這是在告訴 Google「我的應用程式要跟使用者要什麼權限」。

1. 左上角 ☰ → **API 和服務** → **OAuth 同意畫面**
2. User Type 選 **外部 (External)**，按 **建立**
3. 填寫必填欄位：
   - **應用程式名稱**：例如 `世界觀架構工作台`
   - **使用者支援電子郵件**：選你自己的信箱
   - **開發人員聯絡資訊**：填你自己的信箱
4. 按 **儲存並繼續**

### 3-1. 加入權限範圍

1. 在「範圍」這一頁，按 **新增或移除範圍**
2. 在搜尋框輸入 `drive.file`
3. 勾選這一條：
   ```
   .../auth/drive.file
   ```
   說明會寫「查看及管理您使用這個應用程式開啟或建立的 Google 雲端硬碟檔案」
4. 按 **更新**，再按 **儲存並繼續**

> 只要 `drive.file` 就夠了。這個範圍屬於**非敏感**，不需要送 Google 審查。
> 它也代表這個白板**只碰得到自己建立的那個檔案**，看不到你雲端硬碟裡的其他東西。
> 千萬不要選成完整的 `.../auth/drive`，那個會要求審查，而且權限大得多。

### 3-2. 加入測試使用者

1. 在「測試使用者」這一頁，按 **ADD USERS**
2. 填入**你自己的 Google 信箱**
3. 按 **儲存並繼續**

> 應用程式停在「測試」狀態時，只有這份名單上的帳號可以使用，這對自己用剛剛好。
> 如果之後要給別人用，再到 OAuth 同意畫面按「發布應用程式」即可——
> 因為只用了非敏感範圍，發布不需要通過審查。

---

## 4. 建立 OAuth 用戶端 ID

1. 左上角 ☰ → **API 和服務** → **憑證**
2. 上方按 **+ 建立憑證** → **OAuth 用戶端 ID**
3. **應用程式類型**選 **網頁應用程式**
4. 名稱隨便取，例如 `worldbuilder web`
5. 找到 **已授權的 JavaScript 來源**，按 **+ 新增 URI**，填入：
   ```
   https://evonne93327-create.github.io
   ```
   > 只填到網域為止，**後面不要加路徑**，也不要有結尾斜線。
6. 「已授權的重新導向 URI」**不用填**（這個白板用的是不需要轉址的流程）
7. 按 **建立**

---

## 5. 複製用戶端 ID

建立完會跳出一個視窗，顯示「您的用戶端 ID」，長得像：

```
123456789012-abcdefghijklmnop.apps.googleusercontent.com
```

把它複製起來。（旁邊的「用戶端密鑰」用不到，也不要貼進白板。）

---

## 6. 回到白板設定

1. 打開白板，點左側的 **☁️**
2. 上方切到 **Google 雲端硬碟**
3. 把用戶端 ID 貼進去，按 **儲存**
4. 按 **授權 Google 雲端硬碟**，在跳出的視窗選擇你的帳號並同意

> 授權時可能會看到「Google 尚未驗證這個應用程式」的警告。
> 因為這是你自己建立、只給自己用的應用程式，屬正常現象：
> 點 **進階** → **前往「<你的應用程式名稱>」(不安全)** 即可繼續。

完成後，雲端硬碟裡會出現一個 `worldbuilder_data.json` 檔案，
之後你在白板改的任何東西都會自動存進去。

---

## 疑難排解

**「已封鎖存取權：授權錯誤」／ redirect_uri_mismatch**
第 4 步的「已授權的 JavaScript 來源」沒填對。確認是 `https://evonne93327-create.github.io`，
沒有多結尾斜線、沒有多路徑。改完要等幾分鐘才生效。

**「這個應用程式尚未通過 Google 驗證」**
正常現象，見第 6 步的說明。若無法繼續，確認第 3-2 步有把自己的信箱加進測試使用者。

**「載入 Google 登入元件失敗」**
擋廣告的擴充功能可能把 Google 的登入腳本擋掉了。把這個網站加入白名單再試。

**授權視窗一閃而過就關掉**
瀏覽器把彈出視窗擋掉了。允許這個網站顯示彈出式視窗再試一次。
